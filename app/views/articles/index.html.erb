<h1>List of Articles</h1>
<%= link_to 'Create a New Article', '/articles/new', class: 'new-article' %>

<% @articles.each do |article| %>
  <li><%= link_to article.title, "/articles/#{article.id}", class: 'article-title' %></li>
<% end %>
